// MenuBarIcon.styled.ts

import { styled } from '@mui/material/styles';

export const StyledIcon = styled('span')(({ theme }) => ({
  display: 'flex',
  alignItems: 'center',
  color: theme.colors.black_Transparent_8,
}));
